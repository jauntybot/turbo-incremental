[package]
name = "exo-observation"
version = "0.2.3"
edition = "2024"

[dependencies]
turbo = { version = "5.0.0-alpha.0", package = "turbo-genesis-sdk" }
once_cell = "1.17"

[lib]
crate-type = ["cdylib"]

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(no_run)'] }
